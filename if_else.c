#include <stdio.h>
    int main() {

        int number ;  

        printf("10");   
        scanf("%d", &number);
        
        if (number > 5) {
            printf(" 10 is bigger than 5 \n");  
        } else {
            printf("10 is less than 0");       
        }

        return 0;
    }
